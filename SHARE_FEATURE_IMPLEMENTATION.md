# پیاده‌سازی قابلیت ارسال دعوتنامه

## تغییرات انجام شده

### 1. Route جدید (`src/routes/userRoutes.js`)
- اضافه شدن route جدید `POST /sendInvite` برای ارسال دعوتنامه به دوستان
- این route نیاز به احراز هویت دارد و از CSRF protection استفاده می‌کند

### 2. Controller جدید (`src/controllers/userController.js`)

#### متد `sendInvite`:
- اعتبارسنجی شماره موبایل ورودی
- بررسی محدودیت ۲۰ دعوتنامه برای هر کاربر
- جلوگیری از ارسال دعوتنامه تکراری به یک شماره
- جلوگیری از دعوت خود کاربر
- ذخیره شماره در لیست `inviteList` کاربر
- آماده برای ارسال پیامک (خط کد کامنت شده است)

#### به‌روزرسانی متد `getHome`:
- بارگذاری لیست دوستان کاربر از دیتابیس
- محدود کردن به ۲۰ دوست اول
- ارسال اطلاعات دوستان به view

### 3. View (`src/views/campaign/home.ejs`)

#### بخش HTML:
- نمایش دینامیک لیست دوستان از دیتابیس
- نمایش نام یا شماره تلفن و امتیاز هر دوست
- نمایش پیام مناسب در صورت نداشتن دوست

#### بخش JavaScript:
- هندل کردن ورودی شماره تلفن (فقط اعداد فارسی و انگلیسی)
- محدود کردن طول شماره به ۱۱ رقم
- فعال/غیرفعال کردن دکمه ارسال بر اساس ورودی
- ارسال درخواست AJAX به سرور
- نمایش پیام‌های خطا و موفقیت
- نمایش تعداد دعوتنامه‌های باقی‌مانده
- رفرش خودکار صفحه پس از ارسال موفق

## محدودیت‌ها

1. **حداکثر ۲۰ دعوتنامه**: هر کاربر می‌تواند حداکثر ۲۰ دعوتنامه ارسال کند
2. **عدم تکرار**: نمی‌توان به یک شماره بیش از یک بار دعوتنامه ارسال کرد
3. **اعتبارسنجی**: شماره باید یک شماره موبایل معتبر ایرانی باشد

## نحوه فعال‌سازی ارسال پیامک

برای فعال‌سازی ارسال واقعی پیامک، خطوط زیر را در `src/controllers/userController.js` از حالت کامنت خارج کنید:

```javascript
const { done } = await sendSms(text, phone);
if (!done) {
  return res.json({ status: 502, message: "خطا در ارسال پیامک!" });
}
```

## تست

برای تست این قابلیت:

1. وارد صفحه `/home` شوید
2. روی تب "دوستات رو دعوت کن" کلیک کنید
3. شماره موبایل دوست خود را وارد کنید
4. روی دکمه "ارسال دعوتنامه" کلیک کنید
5. پیام موفقیت یا خطا را مشاهده کنید
6. لیست دوستان به‌روزرسانی می‌شود

## ساختار دیتابیس

### فیلدهای استفاده شده در مدل User:
- `inviteList`: آرایه‌ای از شماره تلفن‌های دعوت شده
- `friendList`: آرایه‌ای از ID کاربران دوست
- `shareLink`: لینک اختصاصی کاربر برای دعوت

## نکات امنیتی

1. استفاده از CSRF Token برای جلوگیری از حملات CSRF
2. اعتبارسنجی شماره تلفن در سمت سرور
3. محدودیت تعداد دعوتنامه برای جلوگیری از spam
4. استفاده از middleware احراز هویت

